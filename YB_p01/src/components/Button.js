import React from 'react';
import { Edit } from './sub/Icon';

export default function CardButton(props) {
    return (
        <button>
            <div>
                <Edit />
            </div>
        </button>
    );
}